extends layout.pug
include utils.pug

block content
    if !allStories.length
        span no stories...
    else
        each post in allStories
            +storyDisplay(post.username, post.image, post.content, post.likes)
    script(src="./javascripts/likes.js")
